import './index.css';

export const PrimaryButton = ({ children }) => <button className='primary'>{children}</button>;

export const SecondaryButton = ({ children }) => <button className='secondary'>{children}</button>;
